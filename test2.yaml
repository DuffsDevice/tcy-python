my_variable_test:
  key_1:
    - 42
  key_2:
    - 100

append:
  $suffix:
    $value:
      "${value}${suffix}"

do:
  $func:
    $wert:
      ${func.$wert}

function_call: ${:do.${:append._index}}

if:
  True:
    $v:
      $: $v
  False:
    $:
      $v: $v

# Definition of fac.n
fac:
  1:  1
  $n: $n * ${.$n-1}

fac2:
  $n: ${:if.$n==1.$n.$n*${.$n-1}}